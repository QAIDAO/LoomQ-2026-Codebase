# 把这个文件复制成 .env（同一目录），填好值，然后运行 start.ps1 / start.sh。
# .env 已经在 .gitignore 里，不会被提交。
#
# 全部留空也能跑：内置参考模拟器不需要任何配置，三个现成的例子完整可用。
# 下面两组分别解锁「用中文自由提问」和「送到真实量子计算机上跑」。

# --- 自然语言入口（L2 智能体）------------------------------------------
# 任何兼容 OpenAI Chat Completions 的服务都行。以 DeepSeek 为例：
LOOMQ_LLM_BASE_URL=https://api.deepseek.com
LOOMQ_LLM_API_KEY=
LOOMQ_LLM_MODEL=deepseek-chat

# --- 真实量子计算机（量旋云）-------------------------------------------
# 1. 到 https://cloud.spinq.cn 注册账号
# 2. 运行 python tools/make_spinq_key.py 生成密钥对
# 3. 把生成的公钥贴到网站的账号设置里
# 4. 把用户名和私钥路径填在下面
LOOMQ_SPINQ_USERNAME=
LOOMQ_SPINQ_KEYFILE=
