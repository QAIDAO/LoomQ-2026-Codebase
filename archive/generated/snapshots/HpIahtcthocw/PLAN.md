# LoomQ 参赛作战计划（单人）

截止：**2026-08-25 12:00 UTC+8**（以 GitHub Issue 的 `created_at` 为准）
本机只做方案与代码，**所有安装与运行在另一台机器上进行**，见 [RUNBOOK.md](RUNBOOK.md)。

## 一、得奖策略：主攻专项奖，总分顺路一起拿

主线目标是 **「LoomQ 最佳包容性设计与优秀体验奖」**。它的评选标准是：

> 让一位没有任何量子背景的跨界创作者，在 5 分钟内依靠智能体引导，成功在真实量子机上完成人生第一个实验，并理解其科学原理。

这个奖比的是产品、引导和叙事，正是硬核队伍最不上心的地方，而且**不要求总分排名靠前**。评选资格是"L2 智能体交互、新手引导体验、无门槛可视化方向得分突出"。

由此确定投入分配：

| 模块 | 目标 | 分 | 状态 |
|---|---|---:|---|
| L1 通用中间层 | 三平台 + 8 电路 | 35 | 已完成（三后端 24 组合全过） |
| L1 真机（量旋） | 两个平台 | +10 | 已拿满 |
| L2 智能体客观 | 打满 | 20 | 已完成（对标组 24/24） |
| L2 交互体验 | 打满 | 10 | 网页 + CLI 双入口完成，待找真人试 |
| 工程与产品化 | 打满 | 10 | 一键跑通 + 已答"为谁而做"必答题 |
| L3 混合编译 | 打满 | 15 | 已完成（见第七节） |
| Bonus 量子 RISC-V 扩展指令 | 三件交付物齐备 | +8 | 已完成（见第七节） |
| Bonus 新手引导与视觉叙事 | 打满 | +4 | 已完成 |

**L3 的判断改过一次，这里记下为什么。**

原计划写的是"L3 放弃，单人写编译器投产比最差"。那个判断在当时是对的——
L1 和 L2 都还没落地，把两周里最稀缺的时间押在一个可选模块上不合理。
但 L1 三平台加真机、L2 客观分打满之后，L3 变成**唯一还能加分的地方**
（15 分主体加 8 分 Bonus，占剩余全部空间），而且它的判定原话是
"完全确定性、无主观分"，不看评委口味。

重新估工时也发现原判断高估了难度：经典块的文法全部内容是整数字面量、
`r1..r9` 九个变量、四个算符（`+ - == !=`）、`if/else`、顺序赋值，
目标指令集只有 7 条，官方模拟器 170 行。这不是 LLVM，是一个玩具表达式
语言的递归下降解析加直译式代码生成。真正的风险不在代码量，
在题面若干格式未定义——应对办法是一律支持超集，见第七节。

## 二、里程碑

| 阶段 | 产出 | 判定标准 | 状态 |
|---|---|---|---|
| M0 环境 | Python 3.10 venv + braket/spinqit 装好 | 官方示例能跑出 counts | **已完成** |
| M1 位序标定 | 确认每个后端的原生位序 | `tools/probe_bitorder.py` 报告与大赛约定一致 | **已完成**（两后端均需反转） |
| M2 L1 入门（资格线） | spinq + braket 两个模拟器跑通公开电路 | `evaluator.py --level l1 --target spinq,braket` 全 PASS | **已完成**（4/4 PASS） |
| M3 L1 进阶 | 加 originq；跨 12 门覆盖 | `tools/run_regression.py` 六个电路保真度 ≥0.97 | **已完成**（三后端 18 组合全过） |
| M4 L2 客观 | `agent_chat` 三类任务 | 三类任务在真实模型下通过 | **已完成**（对标组 24/24，见下） |
| M5 L2 交互 | 零基础用户入口 | 找一个不懂量子的人，5 分钟内跑出第一个电路 | 网页 + CLI 双入口完成，待找真人试 |
| M6 真机 | 量旋云跑一次，存 job_id + result.json | job_id 可在控制台溯源 | **已完成**（两个平台，见下） |
| M7 提交 | 证据包 + Issue | 拿到 `submission:accepted` 标签和回执 | 证据包骨架已写 |
| M8 L3 混合编译 | `compile_hybrid` 过随机用例 | 随机程序穷举注入差分全过 | **已完成**（800 组，见第七节） |
| M9 Bonus 量子扩展指令 | 规格 + fork 模拟器 + 端到端测试 | 三者齐备且测试全绿 | **已完成**（见第七节） |

**M2 是生死线，已经过了。** 12 分资格线到手，意味着后面所有专项奖的评选资格都保住了。

剩下的只有 M5 的**真人实测**这一件，因为它只能由人来做。
记录模板已备好：`starter_kit/evidence/five-minute-test.md`。

### 为什么补了网页入口（2026-08-10）

原先只有 CLI，判据其实过不去。那句标准里的主语是「没有任何量子背景的
**跨界创作者**」——让这样一个人先装 Python、再在终端里敲
`python -m loomq.cli`，这件事本身就跟"零门槛"的主张相冲突：终端本身就是门槛。
实测还撞上一个更直接的证据：Windows 控制台默认按 GBK 输出，开场引导整屏乱码
（已在 `cli._ensure_utf8_output()` 修掉），而这类问题在浏览器里根本不存在。

于是补了 `loomq/web.py` + `loomq/webui/`，`start.ps1` / `start.sh` 一键起服务
并自动打开浏览器。三个刻意选择：

1. **不引入任何新依赖**，只用标准库 `http.server`。requirements.txt 里
   braket / spinqit / pyqpanda 三家的版本约束已经互相咬得很紧（见第二节），
   为了一个界面引入 Flask 或 FastAPI，风险远大于收益。
2. **真机走异步任务 + 轮询**。真机排队约两分钟，同步请求必然超时，而且用户
   盯着转圈什么也看不到。排队、提交、取结果逐条实时显示——等待过程本身就是
   "这是真机不是模拟器"最有力的证据。
3. **电路布局在后端算**，前端只画。布局规则已经在 `visualize.circuit_diagram`
   里验证过，用 JavaScript 重写一遍等于把踩平的坑重新挖开。

界面按那句判据逐项搭：开场三句话 → 三个现成示例 → 中文自由提问 →
真机对照（实心条是实测、虚线是理想值）→ **三道原理验收题** → 完成凭证。
最后两项是专门冲着判据尾巴那半句"并理解其科学原理"去的——跑通不算数，
能说清楚才算，而验收结果连同用时一起写进凭证，是这条判据唯一可留存的证据。
右上角计时器把"五分钟"这个标准摆到台面上，让它可被当场证伪。

### 三平台全部打通（2026-08-08）

`pyqpanda` 3.8.5 装上后没有引入任何冲突——**现有包一个都没被改版或移除**，只新增
14 个包。三后端 × 六个隐藏回归电路共 18 个组合保真度全部 ≥0.987，
`evaluator.py --level l1` 从 4/4 变成 6/6。

`backends.py` 里 originq 那条路径**原先是没实测过的，一接就暴露三处错**，
每一处都只在真跑时才现形，靠读文档发现不了（见第四节陷阱 6）。

### 真机实测（2026-08-08）

`gemini_vp`（2 比特 NMR）与 `triangulum_vp`（3 比特 NMR）两个平台都已实跑，真机 10 分拿满。
账号可见但当前无机在线的还有 `hercules_vp`(5)、`superconductor_vp`(8)、`simulator`(24)。

**判定标准与直觉相反，值得记下来。** 真机这 10 分**只查主峰命中，不查保真度**，
题面原话是"真机允许噪声，只查主峰命中"。0.97 阈值属于 L1 语义等价那 35 分，
跑在无噪声模拟器上。我们的真机保真度只有 0.7743（Bell）和 0.6233（GHZ-3），
但 Top-2 主导态完全正确且分离度 5.3 / 5.7 倍，三条标准全过。
`tools/check_hardware_evidence.py` 按这三条逐条核验，避免拿错标准去优化不得分的地方。

**第 3 条的溯源性已按编号实测关掉。** `tools/fetch_spinq_raw.py` 重新登录平台、
按编号逐个取回任务，四个编号（含两个位序标定探针）全部取回成功。
原本计划补的"控制台任务页截图"这条路走不通也不必走了——网页控制台的「我的实验」
两个页签都是 0 条，那个列表只收录网页电路设计器创建的实验，不含 SDK 提交的任务。

### 陷阱 9：交付的 result.json 不可能既是"原始"的又满足统一 Schema

题面要"真机返回的原始 `result.json`"，但官方 `validate_schema` 同时要求
`bit_order == "little"` 且 counts 总和**严格等于** `shots`。真机的实际情况是：

| 冲突点 | 平台实际返回 | Schema 要求 |
|---|---|---|
| 位序 | 原生位串以 q[0] 为最左字符 | 最右字符须为 c[0] |
| 总数 | `S-260808-0001/0002` 只返回 **1023** 次 | 严格等于 shots=1024 |

照抄原始载荷会 schema 非法、整个 case 判 0；而归一化之后就不再是"原始"的了。
两个要求在这道题里没法同时满足。

解法是两份都交，并给出**可复算**的对照：`spinq-cloud-raw-payloads.json` 存平台原始返回，
`fetch_spinq_raw.py` 自动核对"申报文件是否等于原始载荷走一遍归一化"，实测逐位一致。

这一条的实际风险不在分数判定，在**观感**：评委按编号复核时会看到位串是反的、
`000` 差 1。一个正在查作弊的人看到这个，第一反应是数据被动过。
预先把变换摊开讲，能把疑点变成严谨的证据。

## 三、架构：一个关键的解耦

题面要求"真正抽象的中间层，三套硬编码分支不算通用，评委会审查架构"。同时注意 **`transpile()` 和 `run()` 的消费者不是同一个**：

- `transpile()` 的返回值由**组委会的解析器**打分（`target_ir_contract.md` 规定了规范子集）
- `run()` 的内部实现只需要让**真实 SDK** 跑出正确 counts

这两者的门名可以不同。例如 Braket 的 OpenQASM 方言用 `si`/`ti`/`ccnot`，而 stdgates.inc 用 `sdg`/`tdg`/`ccx`。所以：

```
QASM2 文本
   ↓ qasm2_parser
统一 IR（Circuit: n_qubits / n_clbits / ops[Gate|Measure]）
   ├─→ emit_qasm2   → target "spinq"    （SpinQit 原生吃 QASM 2.0，转译与执行同一份）
   ├─→ emit_qasm3   → target "braket"   （交给评测器的规范 OpenQASM 3）
   └─→ emit_originir→ target "originq"  （OriginIR 文本）
                              ↓
                       backends.run_*  （真正执行，可用各自方言或 SDK 原生 API）
                              ↓
                    counts 归一化（位序 + Schema）
```

门名映射全部集中在 `emitters.py` 的字典里，加后端只加一张表，这就是"抽象中间层"的可审查证据。

完整模块清单（都在 `starter_kit/loomq/`）：

| 模块 | 职责 | 依赖 |
|---|---|---|
| `ir.py` | `Circuit`/`Gate`/`Measure`，唯一的内部数据结构；输入白名单 12 门与 IR 门集（含 `u1`）严格分开 | 无 |
| `qasm2_parser.py` | OpenQASM 2.0 → 统一 IR；多寄存器展平、整寄存器广播、白名单外的门直接拒绝 | 无 |
| `emitters.py` | 统一 IR → 各后端原生表示，门名映射集中于此 | 无 |
| `decompose.py` | `gate_identities.md` 的门分解，落点为 `h`/`x`/`cx`/`rz`/`u1` | 无 |
| `refsim.py` | 纯标准库参考态矢模拟器，供离线验证与 Agent 自验 | 无 |
| `counts.py` | 位序与结果 Schema 归一化；`coerce_to_counts` 处理 SDK 返概率而非计数的情况 | 无 |
| `backends.py` | 真实 SDK 执行层，**惰性导入**——缺哪个 SDK 只影响那一个 target | 各 SDK |
| `agent.py` | L2 智能体：意图生成 / 代码纠错 / 选后端，含自验重试闭环 | LLM 服务 |
| `visualize.py` | 电路图 / 直方图 / 人话解释，字符集自动降级 | 无 |
| `cli.py` | 零基础用户入口 | 无 |

只有 `backends.py` 与 `agent.py` 需要外部依赖，其余全部纯标准库。这是"永远能跑起来"的结构性保证：本机无 SDK 无 key，仍能完整验证除真机执行外的一切。

## 四、九个必须重视的技术陷阱

本节是 L1/L2 阶段踩到的 1–6 条。另外三条按内容就近放在了别处：
陷阱 7、8 在第七节（L3），陷阱 9 在上面第二节的真机实测部分。

前三个结论**全部经 `tools/selftest_decompose.py` 用参考模拟器数值验证**，第四个由真实模型实测抓出，第五个由真机实测抓出，第六个由接第三个平台时实测抓出。都不是照抄文档——事实上第六个恰恰是"照抄文档会错"的例子。

### 1. 受控相位门认错语义会让 QFT-4 静默失败

`gate_identities.md` 说"cu1 分解必须用 u1，作为受控门分解的组成部分时 rz 不可互换"。实测结论比这更精确，而且要点不在 rz 上：

**把 cu1 那个 5 门分解里的三个 `u1` 全部换成 `rz`，结果只差一个全局相位，完全等价。** 因为单比特 `rz(φ) = e^{-iφ/2}·u1(φ)`，这个因子是纯标量；标量与一切算子交换，三个标量相乘仍是标量，任何测量都观测不到。

真正会错的是把 `cu1` 实现成**受控 rz**（crz）：

```
crz(θ) = diag(1, 1, e^{-iθ/2}, e^{iθ/2})
cu1(θ) = diag(1, 1, 1,        e^{iθ})
```

此时相位挂在受控分支上，成了**相对**相位，可观测。实测在 QFT 风格的两比特电路上保真度只有 **0.8154**，在完整 QFT-4 上只有 **0.7241**，都远低于 0.97 阈值。而 Bell 和 GHZ 不含 `cu1`，完全看不出问题。

落到实操：各平台的受控相位门要认准语义是 `diag(1,1,1,e^{iθ})`。Braket 用 `cphaseshift`（正确），OpenQASM 3 stdgates 用 `cp`（正确），OriginIR 契约写的是 `CU1/CR`——**`CR` 是否为 cu1 语义需要在接本源时实测确认**。永远不要用"受控 rz"去顶替 `cu1`。

### 2. 位序错误对大多数"标志性"电路完全隐形

`bell.qasm` 的理想分布是 `{"00":0.5,"11":0.5}`，`ghz3` 是 `{"000":0.5,"111":0.5}` —— **两者都是回文对称的**，把整个位串反转过来分布一模一样。位序写错时公开自测依然全 PASS。

更值得警惕的是：**GHZ-5 和 Grover-3 也测不出位序错误**（`gen_circuits.py` 的灵敏度检验实测保真度均为 **1.0000**）。GHZ-5 的原因同 bell；Grover-3 的原因是目标态 `111` 本身回文，而其余七个态概率相等，反转只是把它们互相置换。所以"Grover 命中率 94.5%"看着很有辨识度，其实对位序毫无约束力。

真正能抓住位序错误的是 **QFT-4（反转后保真度 0.1340）和三个随机电路（0.38 / 0.57 / 0.60）**——它们的分布不对称。

不过回归集只是兜底。位序应当**先标定、再回归**：`tools/probe_bitorder.py` 用非对称探针电路（只翻转 `q[0]`）直接读出后端的原生约定。大赛约定 key 的最右字符是 `c[0]`，所以 2 比特下正确结果必须是 `"01"`，若得到 `"10"` 说明该后端原生位序相反，需在 `counts.py` 里反转。

### 3. 对基态做 QFT 是无效的回归测试

QFT 作用在单个基态 `|x⟩` 上时，输出的 16 个振幅**模长全部相同**，测量分布是均匀的 6.25%，与 x 取值无关，也与相位是否算错无关。用这种电路做回归，任何相位错误都测不出来（实测确认）。

`tools/gen_circuits.py` 因此用 `ry(pi/2)` 制备输入态——不用 `h`，因为 QFT 自身也对每个比特施加 `h`，用 `h` 制备会与之相互抵消，实测会把分布搅成 11 个杂峰。制备在 `q[0]`、`q[1]` 上，输出恰好是 4 个 25% 的峰，与题面对 QFT-4 的描述一致。

同样的道理也否掉了随手取的随机电路种子：原先 `random_b`/`random_c` 的种子生成的分布接近均匀（`random_c` 是 32 个态各 3.125%），**对相位错误和位序错误的保真度都是 1.0000**，等于白测。现在的三个种子是筛出来的，要求两类错误都能检出。`gen_circuits.py` 末尾的灵敏度检验会把每个电路对两类错误的保真度列成表，任何一类没有探测器就报警。

### 4. 自验回路会把正确答案改错——态族名天生欠定

L2 的核心工程是"生成 → 自验 → 不对就重试"。但自验的基准如果取错，这个闭环会**反向工作**：它不是修错，而是把对的改成错的。这是接上真实模型后立刻抓到的，`tools/selftest_l2_live.py` 24 个对标用例里有 1 个栽在这。

事故经过：prompt 要求"两比特电路，测量结果只可能是 01 或 10，各占一半"。模型首轮给出的电路（`h q[0]; cx q[0],q[1]; x q[1];`）和它自己声明的分布 `{01:0.5, 10:0.5}` **都是对的**，但它把 `target_family` 标成了 `"bell"`。旧实现把 `"bell"` 硬解释为 |Φ+⟩ = `{00,11}`，据此判定保真度 0.0000、否决电路，再把"你应该落在 00/11"作为反馈喂回去——模型照办，第二轮交出一个真正错误的贝尔态。

根因是**族名只能定到"族"，定不到唯一成员**：贝尔态有四个（Φ±、Ψ±），其中 Φ± 测量得 00/11，Ψ± 测量得 01/10；GHZ 在局部翻转下同样不唯一。模型把 Ψ+ 叫 "bell" 在物理上没错，错的是我们假定这个名字唯一对应一个分布。

修法不能简单地"改成信模型声明的分布"——那会拆掉防伪造守卫（模型编个错电路再编个配套的错期望就能蒙混过关）。麻烦在于这两种情形**在代码视野里结构完全同形**：都是"电路与声明一致、与族名冲突"。唯一能区分它们的信息是**用户原话**，而那不在验证函数的视野里。

所以拆成三态（`classify_verification` 返回 `pass` / `label_conflict` / `fail`）：冲突时不擅自判决，而是带着用户原始请求和 refsim 精确算出的**实际分布**去做一次定向裁决——问的是"这个分布是否满足用户要求"，不是"你的标签对不对"。分布由我们独立计算，模型无从粉饰；用户原话这个唯一外部基准也被带了进来。裁决说不满足就退回正常重试，任何异常一律按不满足处理，只用来救回正确答案，不用来放宽标准。

配套还加了两层：重试时保留历史最优版本（避免越改越差），以及在 system prompt 里把族名定义从"俗称"改成"按测量分布判定"。

顺带修掉了离线测试里的一个假阳性：`selftest_agent.py` 那个"自洽的假期望"用的是大端序位串 `{000,110}`，而 `GHZ3_BAD` 的真实分布是小端序的 `{000,011}`——两者对不上，所以这条防伪断言一直是靠巧合通过的，真正的自洽造假路径从未被测到。现已改正，并补了两个标签冲突用例（救回 / 否决各一）。

### 5. 从带噪声的真机分布里推不出"哪些结果是信号"

接真机后新增的一个陷阱，性质和第 4 个一样：**用启发式代替基准，在多数样本上看起来能用。**

CLI 要向零基础用户解释真机结果，最初的实现完全数据驱动——把"概率达到最高峰一半以上"的结果认作主峰、其余算噪声。第一次真机运行（Bell，任务 `G-260808-0003`，45%/44%）表现正常。

第二次运行（`G-260808-0004`）就打穿了它：同一个电路跑出 **59%/29%**，29% 差一点没够到一半线，于是 `11` 被判成噪声——界面既丢掉了"这就是纠缠"这句话，又反过来宣称"40% 落在 `00` 之外"。真机批次间波动就是这么大。

根因不是阈值没调好，而是**光看一个带噪声的分布，原理上就无法区分信号与噪声**，调阈值只是把失败推到下一次。修法是不猜：`refsim` 本来就能精确算出该电路的理想分布，拿它当基准——结构性解释讲理想的（电路本该做什么），偏差解释讲实测的（真机差在哪、差多少）。并列显示两份分布反而成了最好的教具，正对专项奖标准里"理解其科学原理"那一句。

`explain_distribution` 里保留的主峰识别也改成**清晰可分才下结论**（切点处前一名须达后一名 3 倍），证据不足时宁可少说一句。`tools/selftest_explain.py` 用那组真实坏批次数据钉住了这个回归。

### 6. 契约"允许"的写法与 SDK "接受"的写法不是同一个集合

接第三个平台时，`emit_originir` 一次暴露三处错。三处都符合官方契约的字面描述，
但 pyqpanda 3.8.5 全都不接受——**只在真跑时才现形**。

| 我们原先发的 | pyqpanda 的反应 | 实际要发 |
|---|---|---|
| `RY(1.5708) q[0]` | `no viable alternative at input 'RY('` | `RY q[0],(1.5708)` — 参数在操作数**之后** |
| `CU1 q[0],q[1],(θ)` | `UserDefinedGate CU1 undefined` | `CR q[0],q[1],(θ)` |
| `SDAG q[0]` | `UserDefinedGate SDAG undefined` | `DAGGER` / `S q[0]` / `ENDDAGGER` 块 |

契约原话是「参数门同时接受 `RY(θ) q[0]` 与 `RY q[0],(θ)`」、门名列表写「`CU1/CR`」，
所以前两处直接统一用能真跑起来的那种写法，两边都成立，不需要维护两套方言。

第三处不行：`DAGGER` 是**结构而非门名**，不在契约的允许门名清单里。
所以 `emit_originir` 加了 `executable` 参数——判定输出（`transpile`）仍发契约写明的
`SDAG`/`TDAG`，执行路径才发 `DAGGER` 块。这与 braket 那边「判定发 stdgates、
执行发方言」是同一个模式，成因也相同。

**顺带关掉了一个悬了很久的待确认项。** 陷阱 1 里说 OriginIR 的 `CR`
是否为 `cu1` 语义需要实测。现在实测了：`tools/probe_originir.py` 用一个能区分
`cu1` 与 `crz` 的电路比对，与 `cu1` 保真度 **0.9988**、与 `crz` 只有 **0.7233**，
**`CR` 就是 `cu1` 语义**，可以直接用。QFT-4 在 originq 上 0.9953 也印证了这一点——
若 `CR` 是 `crz`，按陷阱 1 的实测它会掉到 0.72 附近。

位序也是实测标定的：originq 原生位序**与约定一致**（`True`），
而 spinq/braket 都相反。这正是必须逐个标定、不能沿用别家结论的例子。

### L2 实测通过率

工具：`tools/selftest_l2_live.py`，用 `l2_policy.json` 指定的正式模型 `deepseek-v4-flash`（temperature 0、thinking 关闭，与官方一致）。

判定口径对齐题面：生成/纠错用官方 evaluator 的同一个正则抽 QASM，refsim 无噪声模拟后比 Hellinger 保真度，阈值 0.97；选后端的答案集不写死，由 `backend_capabilities.json` 按约束算出。

| 组别 | 结果 |
|---|---|
| 对标组（24 例，对应题面三类任务的措辞/比特数/目标态变体） | **24/24**，连续三轮 100% |
| 进阶组（12 例，刻意超出题面示例难度） | 8/12 |

对标组折算客观分 ≈ 20/20。**这条线是硬的**：10 分的交互体验分只在客观分 ≥ 12 时才计入，也就是正式评测 12 个 case 至少要过 8 个——专项奖的资格因此直接压在 L2 客观分上，它不是加分项。

进阶组剩下的 4 个失败集中在两类：用户显式枚举位串时模型会替换成它更熟的标准态（要 `000/011` 给了 `000/111`），以及自然语言序数指代比特的方向（"第一个比特"）。已经确认这两类**不宜继续靠调 prompt 解决**——实测中修好"指定位串"的同时就弄坏了"序数指代"，进阶组净分不变，而每一次改动都在给已经满分的对标组增加回归风险。

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| spinqit 与 Braket 有三层版本冲突（Python 版本、antlr 硬钉、声明下界不等于真实兼容） | 已解决并锁进 `requirements.txt`：3.10.11 + `braket-sdk==1.97.0` + `default-simulator==1.27.0` + `antlr==4.9.2`。详见 RUNBOOK 步骤 2 |
| Windows 上 `import igraph` 时好时坏 | 缺 `MSVCP140.dll`。装 VC++ 运行库；先 import torch 会"碰巧"成功，别依赖那个 |
| Windows 上 pyqpanda 安装困难 | originq 是进阶项，装不上就先跳过，不影响资格线 |
| 评测在 Linux 容器，本地是 Windows | 提交前用仓库自带 Dockerfile 验一遍 |
| 依赖版本 | `requirements.txt` 必须写 `==`，写 `>=` 会被拒 |
| L2 无 key 可调试 | 自备 DeepSeek key，组委会赛前不提供任何额度 |
| 提交无效 | 建 Issue 不等于提交成功，必须拿到 `submission:accepted` 标签 + 归档回执 |
| Braket 门名方言 | `transpile` 输出与 `run` 内部解耦（见第三节），先用探针脚本确认 |

## 六、当前进度

本机（无量子 SDK、无 API key）能验证的部分全部完成并通过：

- [x] 赛题与 Starter Kit 通读
- [x] 架构设计
- [x] QASM2 解析器 + 统一 IR + 三个发射器 —— `selftest_transpile.py` 37 项断言通过
- [x] 参考态矢模拟器（纯标准库）—— 对上官方公开分布
- [x] 门分解 + 数值验证 —— `selftest_decompose.py` 22 项断言通过
- [x] 隐藏电路回归集 GHZ-5 / QFT-4 / Grover-3 / Random×3 + 理想分布 + 灵敏度检验
- [x] 位序探针脚本
- [x] L2 Agent（三类任务 + 自验重试闭环 + 标签冲突裁决）—— `selftest_agent.py` 42 项断言通过
- [x] L2 真实模型实测 —— `selftest_l2_live.py` 对标组 24/24（`deepseek-v4-flash`）
- [x] 电路图 / 直方图 / 人话解释
- [x] 零基础用户 CLI 入口 —— `python -m loomq.cli --demo` 端到端跑通
- [x] 零基础用户网页入口 —— `start.ps1` / `start.sh` 一键起服务，`selftest_web.py` 74 项
- [x] 证据包与 `submission.yaml`

真实 SDK 环境（Python 3.10.11 + braket 1.97.0 + spinqit 0.2.4）也已装好并跑通：

- [x] 后端执行层实测 —— `backends.py` 三处 `[待实测]` 全部关闭
- [x] 位序标定并写回 `counts.NATIVE_MATCHES_CONTEST` —— 两后端均需反转
- [x] `evaluator.py --level l1 --target spinq,braket` 全 PASS（**评奖资格线已到手**）
- [x] `requirements.txt` 精确锁版本 —— 干净环境 dry-run 验证可解
- [x] 隐藏电路回归跑真实后端 —— 6 电路 × 2 后端保真度 ≥0.989

还差的（都需要外部账号或人，不是代码问题）：

- [x] 量旋云真机 + job_id 证据（两个平台，真机 10 分拿满）
- [x] 真机 job_id 溯源实测复核（`fetch_spinq_raw.py`，四个编号全部取回成功）
- [ ] 找一个不懂量子的人实测 5 分钟引导（模板：`evidence/five-minute-test.md`）
- [ ] 录制演示视频（可选；八张网页截图已在 `evidence/files/webui/`）
- [x] originq / pyqpanda（L1 进阶，三后端 24 组合全过，evaluator 6/6）
- [x] L3 混合编译 + Bonus 量子 RISC-V 扩展指令（见第七节）

### 各模块自测命令一览

```bash
python tools/selftest_transpile.py     # 转译层 37 项，纯标准库
python tools/selftest_decompose.py     # 门分解数值验证 22 项，纯标准库
python tools/selftest_agent.py         # L2 Agent 42 项，本地假端点，无需 API key
python tools/selftest_explain.py       # 结果解释文案 35 项，纯标准库
python tools/selftest_hybrid.py        # L3 混合编译差分测试，纯标准库
python tools/selftest_quantum_ext.py   # 量子 RISC-V 扩展七组测试，纯标准库
python tools/selftest_l2_live.py       # L2 真实模型压测，需 LOOMQ_LLM_* 三个环境变量
python tools/gen_circuits.py           # 重新生成回归集并做灵敏度检验
python tools/probe_bitorder.py         # 位序标定（需装 SDK）
cd starter_kit && python -m loomq.cli --demo
```

六条纯标准库的命令加起来约 20 秒，任何一条报 FAIL 都不要往下走。

## 七、L3 混合编译与量子 RISC-V 扩展

### 分层

```
Hybrid-QASM
   │
   ├─ split_hybrid()      按花括号深度扫描，拆出量子语句与 classical 块
   │                      （不能用正则一把匹配——块里有嵌套花括号，正则数不清配对）
   │
   ├─ 量子部分 ──→ 复用 L1 的 qasm2_parser，一行没另写
   │
   └─ 经典部分 ──→ tokenize()  →  Parser 递归下降  →  AST
                                      │
                                      ├─ interpret()   参考解释器，只用于差分测试
                                      └─ Emitter       → 7 条指令的 RISC-V 汇编
```

实现在 `starter_kit/loomq/hybrid.py`，`adapter.compile_hybrid` 只做契约绑定。
量子部分完全复用 L1 已经验证过的解析器，这是分层的又一次回报。

### 两处关键设计

**一、表达式归一化成仿射式。** 经典块只有 `+` 和 `-`，没有乘除，
所以任何表达式都是 `常数 + Σ(±寄存器)`。先归一化再生成代码，
而不是按 AST 递归。这解决了两个加压测试里真栽过的边界，见下面陷阱 7。

**二、题面文法未定义的边界一律支持超集。** 题面给的是示例加一段自然语言，
不是形式文法：嵌套 if、括号、无 else 的 if、多个 classical 块、寄存器间运算
都没写。而判定说会"随机生成 N 组用例（不同分支结构、不同常量、不同测量位数）"。
递归下降天然支持嵌套与括号，多写几行而已；**支持得比要求多只会更安全，
反过来则直接丢分**。

### 怎么知道扛得住隐藏随机用例

把官方判定方式在本地复刻了一遍：`tools/selftest_hybrid.py` 自己随机生成程序、
自己写参考解释器、穷举注入 2^n 组测量值，比对"解释器算出的终态"与
"生成的汇编在**官方** riscv_emulator.py 上跑出的终态"。800 组随机程序全过。

同错风险是真实的——解释器和代码生成出自同一双手。两条防线：一是两条路径
结构上差得远（树求值 vs 寄存器分配加分支跳转），二是 8 个固定用例的期望值
**全部手算**，独立于两条路径。

### 陷阱 7：公开自测覆盖不到的地方，题面自己的示例就踩了

`evaluator.py --level l3` 只有一个用例：单测量位、只用 `==`、只赋常量，
不测 `+ -`、不测 `!=`、不测顺序赋值、不测嵌套，也不检查量子序列内容
（只检查返回值类型是 `list`）。它全绿之后才发现**题面自己给的那段示例编译不过**
——示例在 `classical {` 后面跟了 `// 经典控制块……`，而公开用例恰好没有注释，
词法器漏掉注释处理也能过公开用例。

这和陷阱 2（位序错误对回文对称电路完全隐形）是同一类毛病：
**公开样例全绿不是证据**，得自己造有鉴别力的用例。

### 陷阱 8：临时寄存器消耗随嵌套深度增长，两个边界一起崩

按 AST 递归生成代码时，每层嵌套要占一个临时寄存器。加压测试打出两个失败：

| 边界 | 现象 | 根因 |
|---|---|---|
| 测量位多 | 18 位还行，**20 位报寄存器不够** | `c[k]` 映射到 x10+k，位数一多就向上吃掉临时寄存器区（x31 往下） |
| 嵌套深 | 生成 **1311 条指令** | 官方模拟器 `max_steps` 只有 1000。这次没触发是因为分支跳过了一部分，属于撞运气 |

两者根因相同，所以一个改动同时解决：归一化成仿射式后，同时活跃的临时寄存器
降到 **1 个**、与嵌套深度无关（20 个测量位也够用）；常数在编译期折叠掉，
最长指令数 1311 → 658、常规用例 231 → 132。比较也顺带简化：
`lhs == rhs` 归一成 `lhs - rhs`，直接跟硬连线为零的 `x0` 比。
两个边界都固化成了回归用例。

### Bonus：量子 RISC-V 扩展指令

规格 `docs/quantum-riscv-extension.md`，实现 `tools/riscv_quantum_emulator.py`，
测试 `tools/selftest_quantum_ext.py`。题面要求三者齐备方计分。

**它解决 L3 主体解决不了的问题。** L3 的产物是两份互不相识的东西——
量子操作序列和经典汇编，评测时也分开验证。但真实混合程序里两者有数据依赖：
测量结果要立刻决定后续经典分支。把量子操作编码成 custom-0 自定义指令后，
两者进入**同一条指令流、在同一台机器上顺序执行**：`q.meas` 把塌缩结果直接
写进通用寄存器，下一条 `beq` 立刻能用它跳转。

现场可跑的判据：`python tools/riscv_quantum_emulator.py` —— Bell 态连跑五个种子，
两个测量位恒相等，经典段算出的 `x1` 恒为 1。塌缩写错的话 q[1] 会独立抽样，
约一半次数就会不等。

三个刻意的选择：

1. **做真编码，不只加助记符。** 题面说的是"设计 custom opcode 编码"，
   所以模拟器先把指令编码成 32 位机器码再解码执行。解码器还校验
   "按规格应为 0 的字段确实为 0"——宁可报错也不要把垃圾位静默执行成别的指令。
2. **量子语义与 L1 同源。** 门的酉变换直接调 `refsim._apply_gate`，不另写一套。
   于是第五组测试比对的其实是"编解码有没有把比特下标放对位置"，
   用的都是非对称电路（高位比特、cx 反向控制、swap、ccx）——
   对称电路把下标放错也照样通过，这是陷阱 2 的同一个教训。
3. **fork 没动坏原语义要有凭据。** 第四组用 L3 的随机程序生成器造 400 组程序，
   逐条比对 fork 与官方模拟器的寄存器终态，全部相同；
   官方文件自带的 demo（期望 `x3=16`）也一字不差地过。

**过程中被测试照出来的一个真 bug：** 三条错误路径的诊断信息用了 `%03b`，
但 Python 的 `%` 运算符不支持 `%b`，格式化自身抛 `ValueError`。
这些错误路径原本会崩在格式化上，给出一句看不懂的 `ValueError`，
而不是想给的那句诊断。**专门测错误路径才照得出来**——
只测正常路径的话，这个 bug 会一直躺在那里。
